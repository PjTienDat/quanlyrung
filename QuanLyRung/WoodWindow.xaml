<Window x:Class="QuanLyRung.WoodWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuanLyRung"
        mc:Ignorable="d"
        
                           xmlns:uc="clr-namespace:QuanLyRung.UserControlDat"
        
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
   TextElement.Foreground="{DynamicResource MaterialDesignBody}"
TextElement.FontWeight="Regular"
TextElement.FontSize="13"
TextOptions.TextFormattingMode="Ideal"
TextOptions.TextRenderingMode="Auto"
        AllowsTransparency="True"
        Background="Transparent"
FontFamily="{DynamicResource MaterialDesignFont}"
   ResizeMode="NoResize"
   WindowStyle="None"
   WindowStartupLocation="CenterScreen"
        
        x:Name="window"
        DataContext="{StaticResource WoodVM}"
         Title="Cây gỗ" Height="750" Width="950">


    <Border CornerRadius="15">
        <Border
          CornerRadius="15"
          BorderThickness="2">
            <Border.BorderBrush>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#462ad8" Offset="0"/>
                    <GradientStop Color="#da34ae" Offset="0.5"/>
                    <GradientStop Color="#8a16c1" Offset="1"/>
                </LinearGradientBrush>
            </Border.BorderBrush>
            <Border.Background>
                <LinearGradientBrush StartPoint="0,1" EndPoint="1,0">
                    <GradientStop Color="#060531" Offset="0"/>
                    <GradientStop Color="#1b1448" Offset="0.75"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <uc:ControlBarUC Tag="{Binding Title, ElementName=window}"></uc:ControlBarUC>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <!--cac muc-->
                    <materialDesign:Card Grid.Row="0" Margin="5" Background="#FF36C3BE">
                <WrapPanel>
                            <TextBox Foreground="Black" Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}" Margin="5 5 15 5" materialDesign:HintAssist.Hint="Loại gỗ" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
                            <TextBox Foreground="Black" Text="{Binding Count, UpdateSourceTrigger=PropertyChanged}" Margin="5 5 15 5" materialDesign:HintAssist.Hint="Số lượng" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
                            <TextBox Foreground="Black" Text="{Binding Unit, UpdateSourceTrigger=PropertyChanged}" Margin="5 5 15 5" materialDesign:HintAssist.Hint="Đơn vị" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
                   
                    <ComboBox Margin="5 5 15 5" DisplayMemberPath="DisplayName" 
          ItemsSource="{Binding SupplierWood}" SelectedItem="{Binding SelectedSupplierWood, Mode= TwoWay, UpdateSourceTrigger=PropertyChanged}" 
          materialDesign:HintAssist.Hint="Cơ sở sản xuất" IsEditable="True" Style="{StaticResource MaterialDesignFloatingHintComboBox}">
                    </ComboBox>
                            <DatePicker Foreground="Black" VerticalAlignment="Center" SelectedDate="{Binding DateInput, StringFormat='dd/MM/yyyy'}" Width="100" Margin="5 5 15 -7" materialDesign:HintAssist.Hint="Ngày nhập" Style="{StaticResource MaterialDesignFloatingHintDatePicker}" />
                        </WrapPanel>
            </materialDesign:Card>

            <!--them xoa sua-->
                    <materialDesign:Card Grid.Row="1" Margin="5" Background="#FF36C3BE">
                <WrapPanel>
                            <Button Background="#FF1E8E37" Command="{Binding AddCommand}" Content="Thêm" Margin="5" Width="100"></Button>
                            <Button Background="#FF1E8E37" Command="{Binding EditCommand}" Content="Sửa" Margin="5" Width="100"></Button>
                            <Button Background="#FF1E8E37" Command="{Binding DeleteCommand}" Content="Xóa" Margin="5" Width="100"></Button>
                            <Button Background="#FF1E8E37" Command="{Binding SearchCommand}" Content="Tìm kiếm" Margin="5" Width="100"></Button>
                        </WrapPanel>
            </materialDesign:Card>

            <!--hien thi-->
            <materialDesign:Card Grid.Row="2" Margin="5" VerticalAlignment="Stretch">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    
                    
                            <Grid>
                                <ContentControl>
                                    <ContentControl.Style>
                                        <Style TargetType="ContentControl">
                                            <Setter Property="Content">
                                                <Setter.Value>
                                                    
                                                    
                                                    <ListView ItemsSource="{Binding List}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}">
                                           <ListView.View>
                                                <GridView>
                                        <GridViewColumn Header="Loại gỗ" DisplayMemberBinding="{Binding DisplayName}"></GridViewColumn>
                                <GridViewColumn Header="Số lượng" DisplayMemberBinding="{Binding Count}"></GridViewColumn>
                                <GridViewColumn Header="Đơn vị" DisplayMemberBinding="{Binding Unit}"></GridViewColumn>
                                 <GridViewColumn Header="Cơ sở sản xuất" DisplayMemberBinding="{Binding SupplierWood.DisplayName}"></GridViewColumn>
                                 <GridViewColumn Header="Ngày nhập" DisplayMemberBinding="{Binding DateInput, StringFormat='dd/MM/yyyy'}"></GridViewColumn>
                                  </GridView>
                                           </ListView.View>
                                
                                
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="{x:Type ListViewItem}">
                                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                                    </Style>
                                </ListView.ItemContainerStyle>
                                
                                
                            </ListView>

                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ShowSearchResults}" Value="True">
                                                    <Setter Property="Content">
                                                        <Setter.Value>

                                                            <ListView ItemsSource="{Binding FilteredWoodList}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}">
                                                                
                                                                
                                                                <ListView.View>
                                                                    <GridView>
                                                                        <GridViewColumn Header="Loại gỗ" DisplayMemberBinding="{Binding DisplayName}"></GridViewColumn>
                                                                        <GridViewColumn Header="Số lượng" DisplayMemberBinding="{Binding Count}"></GridViewColumn>
                                                                        <GridViewColumn Header="Đơn vị" DisplayMemberBinding="{Binding Unit}"></GridViewColumn>
                                                                        <GridViewColumn Header="Cơ sở sản xuất" DisplayMemberBinding="{Binding SupplierWood.DisplayName}"></GridViewColumn>
                                                                        <GridViewColumn Header="Ngày nhập" DisplayMemberBinding="{Binding DateInput, StringFormat='dd/MM/yyyy'}"></GridViewColumn>
                                                                    </GridView>
                                                                </ListView.View>
                                                                
                                                                
                                                                <ListView.ItemContainerStyle>
                                                                    <Style TargetType="{x:Type ListViewItem}">
                                                                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                                                                    </Style>
                                                                </ListView.ItemContainerStyle>
                                                            </ListView>
                                                        </Setter.Value>
                                                    </Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ContentControl.Style>
                                </ContentControl>
                            </Grid>
    
    
                        </ScrollViewer>

            </materialDesign:Card>
        </Grid>
    </Grid>
    </Border>
    </Border>
</Window>
